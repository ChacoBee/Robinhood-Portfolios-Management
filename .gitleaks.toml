title = "Aurum Gitleaks configuration"

[extend]
useDefault = true

[[rules]]
id = "generic-api-key"

[rules.allowlist]
description = "Synthetic connected-runtime key fixtures"
paths = [
  '''^apps/server/tests/integration/(api-read-routes|auth-routes|connect-cli|connected-read-model|docker-compose)\.test\.ts$''',
  '''^apps/server/tests/unit/(api-composition|config|runtime-composition|worker)\.test\.ts$''',
]

[[rules]]
id = "stripe-access-token"

[rules.allowlist]
description = "Synthetic connected-runtime token fixtures"
paths = [
  '''^apps/server/tests/integration/(api-read-routes|auth-routes|connect-cli|connected-read-model)\.test\.ts$''',
  '''^apps/server/tests/unit/(api-composition|config|worker)\.test\.ts$''',
]
